eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/

spring:
  application:
    name: api-base
  datasource:
    url: jdbc:mysql://localhost:3306/clouddo?useUnicode=true&characterEncoding=utf8&USESSl=true
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8888
